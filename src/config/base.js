/**
 * You can put settings that you wish to share between environments into the object below.
 * Usage: { SAMPLE_PATH: localhost:3000 }
 * Calls: import config from 'envConfig' -> config.SAMPLE_PATH will be localhost:3000
 */
export default {};
